<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="labs.stemma.imodify"
    android:versionCode="2"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="labs.stemma.imodify.MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="sensorPortrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="labs.stemma.imodify.DatePicker_activity"
            android:label="@string/title_activity_date_picker"
            android:theme="@style/DialogTheme"
            android:screenOrientation="sensorPortrait" >
        </activity>
        <activity
            android:name="labs.stemma.imodify.TimePicker_activity"
            android:label="@string/title_activity_time_picker"
            android:theme="@style/DialogTheme"
            android:screenOrientation="sensorPortrait" >
        </activity>
        <activity
            android:name="labs.stemma.imodify.NamePicker_activity"
            android:label="@string/title_activity_time_picker"
            android:theme="@style/DialogTheme"
            android:screenOrientation="sensorPortrait" >
        </activity>
        <activity
            android:name="labs.stemma.imodify.ShowImage"
            android:label="@string/title_activity_show_image"
            android:screenOrientation="sensorPortrait" >
        </activity>
    </application>

</manifest>
